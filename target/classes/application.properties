spring.profiles.active=test
spring.jpa.open-in-view=false

# Nome da aplica��o
spring.application.name=ms-pagamentos

# � preciso passar a configura��o eureka.client.servidorUrl.defaultZone
# O localhost 8081, onde o Eureka Server est� recebendo as requisi��es
eureka.client.serviceUrl.defaultZone=http://localhost:8081/eureka

# Define a porta para o Eureka controlar em qual porta vai subir o servi�o
server.port=0
